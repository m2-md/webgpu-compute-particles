{
  "name": "webgpu-compute-particles",
  "version": "1.0.0",
  "description": "GPU-resident particle simulation in WebGPU using WGSL compute shaders: a read_write storage buffer updated in place, workgroup thread dispatch, and zero CPU-to-GPU data readback bottleneck.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "bench": "vite-node bench/cpu-sim.bench.ts"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.60",
    "typescript": "^5.9.0",
    "vite": "^6.0.0",
    "vite-node": "^2.1.0",
    "vitest": "^2.1.0"
  }
}
