{
  "name": "webgpu-compute-particles",
  "version": "1.0.0",
  "description": "WebGPU compute shader ile GPU'da kalan parçacık simülasyonu: storage buffer, dispatchWorkgroups, compute + render zinciri ve readback.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "bench": "vite-node bench/cpu-sim.bench.ts"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.60",
    "typescript": "^5.9.0",
    "vite": "^6.0.0",
    "vite-node": "^2.1.0",
    "vitest": "^2.1.0"
  }
}
